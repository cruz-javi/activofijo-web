PORT=3001
NODE_ENV=development

# Core API backend (Internal URL used by BFF routes on server)
CORE_API_URL=http://localhost:3000

# Core API external URL (if direct client access needed)
NEXT_PUBLIC_CORE_API_URL=http://localhost:3000

# Session cookie encryption key
SESSION_SECRET=super_secret_cookie_encryption_key_32_chars!
